public class MylinkedList {
    LNode start, end;
    int size;

    private class LNode {
	int currentVal, nextVal, prevVal;
    }

    private boolean add (int value) {
        LN
	size++;
    }

    public String toString() {
	String strList = "[";
	for (int i = 0; i < size(); i++) {
	    strList += ;
	}
    }
}
